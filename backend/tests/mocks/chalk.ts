export const red = (msg: string) => msg;
export const blue = (msg: string) => msg;
export const green = (msg: string) => msg;
export const yellow = (msg: string) => msg;
export const gray = (msg: string) => msg;

export default {
  red,
  blue,
  green,
  yellow,
  gray,
};